name: Build LLVM for Jailbreak iOS (Full)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
    - name: Clone iOS SDK
      run: |
        git clone https://github.com/xybp888/iOS-SDKs.git
        echo "IOS_SDKROOT=$(pwd)/iOS-SDKs/iPhoneOS16.5.sdk" >> $GITHUB_ENV
    
    - name: Clone LLVM
      run: |
        git clone --depth 1 https://github.com/llvm/llvm-project.git
    
    # 第一步：編譯 native 工具（完整的 macOS 版本）
    - name: Build Native Tools
      run: |
        MACOS_SDK=$(xcrun --sdk macosx --show-sdk-path)
        echo "Using macOS SDK: $MACOS_SDK"
        
        mkdir build-native
        cd build-native
        
        cmake -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_OSX_SYSROOT="$MACOS_SDK" \
          -DLLVM_TARGETS_TO_BUILD="AArch64" \
          -DLLVM_ENABLE_PROJECTS="clang;lld" \
          -DLLVM_INCLUDE_TESTS=OFF \
          -DLLVM_INCLUDE_EXAMPLES=OFF \
          -DLLVM_INCLUDE_BENCHMARKS=OFF \
          ../llvm-project/llvm
        
        # 建置所有需要的工具
        ninja llvm-tblgen clang-tblgen llvm-config llvm-ar llvm-ranlib
    
    # 第二步：建置 iOS 目標的 LLVM 核心（不包含 runtimes）
    - name: Build LLVM Core for iOS
      run: |
        mkdir build-ios
        cd build-ios
        
        cmake -G Ninja \
          -DCMAKE_BUILD_TYPE=MinSizeRel \
          -DCMAKE_SYSTEM_NAME=iOS \
          -DCMAKE_OSX_SYSROOT="$IOS_SDKROOT" \
          -DCMAKE_OSX_ARCHITECTURES="arm64" \
          -DCMAKE_OSX_DEPLOYMENT_TARGET=16.5 \
          -DCMAKE_SYSTEM_PROCESSOR=arm64 \
          -DCMAKE_CROSSCOMPILING=ON \
          -DCMAKE_C_COMPILER_WORKS=ON \
          -DCMAKE_CXX_COMPILER_WORKS=ON \
          -DLLVM_HOST_TRIPLE=arm64-apple-darwin \
          -DLLVM_DEFAULT_TARGET_TRIPLE=arm64-apple-ios16.5 \
          -DLLVM_TABLEGEN=$(pwd)/../build-native/bin/llvm-tblgen \
          -DCLANG_TABLEGEN=$(pwd)/../build-native/bin/clang-tblgen \
          -DLLVM_CONFIG_PATH=$(pwd)/../build-native/bin/llvm-config \
          -DLLVM_TOOL_LLVM_CONFIG_BUILD=OFF \
          -DCMAKE_MACOSX_BUNDLE=OFF \
          -DLLVM_ENABLE_PROJECTS="clang;lld" \
          -DLLVM_TARGETS_TO_BUILD="AArch64" \
          -DLLVM_INCLUDE_TESTS=OFF \
          -DLLVM_INCLUDE_EXAMPLES=OFF \
          -DLLVM_INCLUDE_BENCHMARKS=OFF \
          -DLLVM_INCLUDE_DOCS=OFF \
          -DLLVM_BUILD_LLVM_DYLIB=OFF \
          -DLLVM_LINK_LLVM_DYLIB=OFF \
          -DLLVM_BUILD_UTILS=OFF \
          -DLLVM_ENABLE_ZLIB=OFF \
          -DLLVM_ENABLE_ZSTD=OFF \
          -DLLVM_ENABLE_TERMINFO=OFF \
          -DLLVM_INCLUDE_RUNTIMES=OFF \
          ../llvm-project/llvm
        
        ninja clang lld llvm-ar llvm-ranlib llvm-strip llvm-objcopy \
              llvm-objdump llvm-nm llvm-size llvm-strings llvm-readelf \
              llvm-addr2line llvm-dwarfdump llvm-cov llvm-profdata \
              llvm-symbolizer llvm-dwp
    
    # 第三步：單獨建置 runtimes（compiler-rt, libcxx 等）
    - name: Build Runtimes for iOS
      run: |
        mkdir build-runtimes
        cd build-runtimes
        
        # 使用剛建置的 iOS clang 來編譯 runtimes
        IOS_CLANG=$(pwd)/../build-ios/bin/clang
        
        cmake -G Ninja \
          -DCMAKE_BUILD_TYPE=MinSizeRel \
          -DCMAKE_SYSTEM_NAME=iOS \
          -DCMAKE_OSX_SYSROOT="$IOS_SDKROOT" \
          -DCMAKE_OSX_ARCHITECTURES="arm64" \
          -DCMAKE_OSX_DEPLOYMENT_TARGET=16.5 \
          -DCMAKE_C_COMPILER="$IOS_CLANG" \
          -DCMAKE_CXX_COMPILER="$IOS_CLANG++" \
          -DCMAKE_AR=$(pwd)/../build-ios/bin/llvm-ar \
          -DCMAKE_RANLIB=$(pwd)/../build-ios/bin/llvm-ranlib \
          -DCMAKE_CROSSCOMPILING=ON \
          -DLLVM_ENABLE_RUNTIMES="compiler-rt;libcxx;libcxxabi;libunwind" \
          -DLLVM_ENABLE_LIBCXX=ON \
          -DLIBCXX_ENABLE_SHARED=OFF \
          -DLIBCXX_ENABLE_STATIC=ON \
          -DLIBCXXABI_ENABLE_SHARED=OFF \
          -DLIBCXXABI_ENABLE_STATIC=ON \
          -DLIBUNWIND_ENABLE_SHARED=OFF \
          -DLIBUNWIND_ENABLE_STATIC=ON \
          -DCOMPILER_RT_ENABLE_IOS=ON \
          -DCOMPILER_RT_BUILD_SANITIZERS=OFF \
          -DCOMPILER_RT_BUILD_XRAY=OFF \
          -DCOMPILER_RT_BUILD_LIBFUZZER=OFF \
          -DCOMPILER_RT_BUILD_PROFILE=OFF \
          -DCOMPILER_RT_BUILD_BUILTINS=ON \
          -DCOMPILER_RT_DEFAULT_TARGET_ONLY=ON \
          ../llvm-project/llvm
        
        ninja runtimes
    
    - name: Package Full Toolchain
      run: |
        mkdir -p package/var/jb/usr/lib/llvm-23/bin \
                 package/var/jb/usr/lib \
                 package/var/jb/usr/include/c++/v1
        
        # 複製 iOS 版本的 LLVM 工具
        cd build-ios
        if [ -d "bin" ]; then
          for tool in bin/*; do
            if [[ -f "$tool" ]] && [[ -x "$tool" ]]; then
              cp "$tool" ../package/var/jb/usr/lib/llvm-23/bin/
            fi
          done
        fi
        
        # 複製 native llvm-config（必須是 macOS 版本）
        cp ../build-native/bin/llvm-config ../package/var/jb/usr/lib/llvm-23/bin/
        cd ..
        
        # 複製 C++ 標頭
        if [ -d "llvm-project/libcxx/include" ]; then
          cp -r llvm-project/libcxx/include/* package/var/jb/usr/include/c++/v1/
        fi
        
        # 複製靜態庫
        cd build-runtimes
        for lib in libc++.a libc++abi.a libunwind.a; do
          if [ -f "lib/$lib" ]; then
            cp "lib/$lib" ../package/var/jb/usr/lib/
          fi
        done
        
        # 複製 compiler-rt builtins
        if [ -d "lib/clang" ]; then
          mkdir -p ../package/var/jb/usr/lib/clang
          cp -r lib/clang/* ../package/var/jb/usr/lib/clang/
        fi
        cd ..
        
        # 建立 symlinks
        cd package/var/jb/usr/lib/llvm-23/bin
        ln -sf clang clang++ 2>/dev/null || true
        ln -sf llvm-ar ar 2>/dev/null || true
        ln -sf llvm-ranlib ranlib 2>/dev/null || true
        ln -sf llvm-strip strip 2>/dev/null || true
        ln -sf llvm-objcopy objcopy 2>/dev/null || true
        ln -sf llvm-objdump objdump 2>/dev/null || true
        ln -sf llvm-nm nm 2>/dev/null || true
        ln -sf llvm-strings strings 2>/dev/null || true
        cd ../../../../../..
        
        tar -czf llvm-23-ios.tar.gz -C package .
    
    - name: Upload Full Toolchain
      uses: actions/upload-artifact@v4
      with:
        name: llvm-23-ios
        path: llvm-23-ios.tar.gz
